# status.json is generated at runtime by the monitoring cron job.
# The committed copy is a template/fallback only.

# Fireclaw venv (created by setup/fireclaw_install.sh)
.fireclaw-env/
# LuciferiClaw venv (created by setup/lucifericlaw_install.sh)
.lucifericlaw-env/

# OpenClaw@URANTiOS-ingest — per-host .env generated by install script
openclaw_ingest/.env

# Python bytecode
__pycache__/
*.pyc

# Remotion (Node tooling under remotion/)
remotion/node_modules/
remotion/out/
remotion/build/
remotion/shorts-queue/
